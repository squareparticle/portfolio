# Default portfolio resources

`banner.json` and `aboutme.json` are the default fallbacks used when a job folder does not provide local versions.

Add the generic fallback resume as `resume.pdf`. The stripped source archive did not contain a resume, so this migration intentionally does not invent one. A job-specific `resume.pdf` always takes precedence.
